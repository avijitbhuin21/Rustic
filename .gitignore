# Dependencies
node_modules/

# Build output
dist/
target/

# Local debugging dumps
log.txt

# Lock files (optional - uncomment if you want to track them)
# Cargo.lock is recommended to track for applications
# bun.lock
# package-lock.json

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Updater signing keys (private key MUST never be committed; keep a backup!)
.updater-signing.key
.updater-signing.key.pub

# Environment
.env
.env.local
.env.*.local

# rustic-server runtime data dir (DB, logs, file-history, secrets.json,
# .session-secret) — created by scripts/run-server.ps1; never commit it.
rustic-data/

# Tauri build artifacts
src-tauri/target/
src-tauri/WixTools/
src-tauri/icons/icon.ico.bak

# Logs
*.log
rustic.log*
npm-debug.log*
.rustic/
references/

# OAuth / IDE bookkeeping
console_logs.log

# Database backups produced before SQLite migrations
*.db.bak.*

# SQLite databases + WAL/shared-memory sidecars (rustic.db lives in the data
# dir, but never commit a stray local DB from anywhere in the tree)
*.db
*.db-wal
*.db-shm

audit-report/
plan.md

.claude/
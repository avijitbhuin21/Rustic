 When the agents on some terminals, it does not automatically close. Sometime it does, most of the time it does not. If the terminal has finished itself, it should close automatically.
 In the chat section for agent we have new chat for this project right there right beside that add a terminal option as well on click a terminal will open on that particular project.
 In the chat section of the agent we have three dots inside that we have agent tools and text slides add export button there as well when we click on export it will export the whole chat history right including the image references like if we have pasted any images those are present in our dot rustic folder so including those image references with the full path not the relative path but the full path as a JSON file chat history.
 when upon granting access to all files it should invalidate the path scope validation as well the path scope validation should only be applied when it is turned off
 The retrying pop-up does not have a retry now button. That is something we have to add.check the image.
 Add mermaid graph support in the chat.
 Add IPY-NB cell support.
 After a longer session, things get a little bit choppy. We need to check why that is happening.
 Please add the Open with Rustic option in the menu as well very similar to VS Code.
 Hey, so when we display a file in our editor, right? Please make sure we are showing the project name right beside that as well Let's separate it by a bullet point so that we know which file this particular file is from which project.

 add a feature where we can basically like in from in the app itself we can add a back-end url let's say if we deploy rustic server somewhere if we paste in the production environment here along with the password that we will be able to use the cloud environment here itself all the executions and everything will happen in cloud we will just use our app very similar to ssh but the explorer and the text everything will be synced to the cloud immediately
 Auto inject, so in a when we are chatting right with our agent, we auto inject some prompts right to like checklist reminder or similar things. Those should not like ideally they are not visible when the session is running. But when we stop the session, reload the app and come back those are visible. That is something we need to address and fix.
 secondly we have another issue that is when we go to goal mode or any specific mode right or use any you know workflows or anything some sort that actually like in our chat we can see that all the paste all the text from that particular workflow or skill got pasted that should not happen those should be visible just as the tag on click we should be able to like you know see the skill details and all but by default it should not get pasted like it will be get pasted in the back end but should not be visible to the user.

the image preview should be like a plane where we can zoom in and out the image just by scrolling on a particular section of the image. currently the image zoom is not working when scrolled, and when it does get zoomed with the button it fits the screen horizontally and then only increases vertically.

git push issue:
Publish failed: git push --progress --set-upstream origin master failed (exit 1): Writing objects: 100% (138/138) Writing objects: 100% (138/138), 85.09 KiB | 4.73 MiB/s, done. Total 138 (delta 12), reused 0 (delta 0), pack-reused 0 (from 0) remote: Resolving deltas: 0% (0/12) remote: Resolving deltas: 8% (1/12) remote: Resolving deltas: 16% (2/12) remote: Resolving deltas: 25% (3/12) remote: Resolving deltas: 33% (4/12) remote: Resolving deltas: 41% (5/12) remote: Resolving deltas: 50% (6/12) remote: Resolving deltas: 58% (7/12) remote: Resolving deltas: 66% (8/12) remote: Resolving deltas: 75% (9/12) remote: Resolving deltas: 83% (10/12) remote: Resolving deltas: 91% (11/12) remote: Resolving deltas: 100% (12/12) remote: Resolving deltas: 100% (12/12), done. To https://github.com/avijitbhuin21/Serpentine.git ! [remote rejected] master -> master (refusing to allow an OAuth App to create or update workflow `.github/workflows/ci.yml` without `workflow` scope) error: failed to push some refs to 'https://github.com/avijitbhuin21/Serpentine.git'
